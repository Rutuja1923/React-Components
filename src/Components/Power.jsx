import React from 'react';
function Power(){
    return (
        <React.Fragment>
            <p>4^3 = {Math.pow(4,3)}</p>
            <p>6^2 = {Math.pow(6,2)}</p>
        </React.Fragment>
    )
};

export default Power;

